# LocalWeb Hunter

評価は高い（＝商売が成立している）のに、Web上の営業基盤が弱い（＝公式HPがない／古い）地域店舗を
発掘し、ホームページ制作営業の見込み客リストと営業優先順位を作るローカル実行ツール。

**APIキーなしで、いますぐ全機能を動かせます。**（モックデータ・コスト $0）

モックモードでは、アプリ自身がサンプル店舗のWebサイトも配信します。
そのため **サイト品質の実測・公開メールの抽出も、本番と同じコードで実際に動きます**
（クローラは robots.txt を確認し、HTMLを解析し、`mailto:` からアドレスを拾います）。
メールのドメインは `.test`（実在し得ない予約TLD）に固定してあります。

```bash
cd localweb-hunter
npm install
npm run dev          # → http://localhost:3000
```

`.env.local` すら要りません。検索 → 一覧 → 店舗詳細 → 営業メモ → CSV出力 → 営業指標まで、
実際のコードパスをそのまま通して確認できます。

---

## これは何をするツールか

単に店舗を集めるツールではなく、**「この店にHPを提案する価値が高い」と判断できる店舗を
上位に並べる**ことを目的にしています。

```
検索条件を入力
  ↓ 推定コストを表示 → あなたが確認して実行
Phase 1: 安い一次スクリーニング（AIもWeb検索も使わない）
  評価・レビュー件数・HP有無でルールベースに絞り込む
  ↓ 重複排除
  ↓ ここを通過した店舗だけが次に進む ← 低コストを成立させている仕組み
Phase 2: 通過分だけの詳細調査
  公式サイト候補の探索 → サイト品質の実測 → 公開メールの抽出 → AI営業分析
  ↓
Lead Score / Website Opportunity Score / 営業優先度 S〜D
  ↓
一覧表示 → 電話番号タップ → 営業トーク確認 → 営業メモ記録 → 成約率の計測
```

---

## 画面

| 画面 | できること |
|---|---|
| `/` 検索 | 地域・範囲・業種・最低評価・最低レビュー数の指定。**実行前に必ず推定コストを表示** |
| `/leads` 一覧 | Lead Score / 優先度 / 評価 / レビュー数でソート。**メールアドレスをその場に表示（コピーボタン付き）**、電話あり・メールありで絞り込み、CSV出力 |
| `/leads/[id]` 詳細 | **「次の一手」**（何をするか・どの切り口で入るか・何を提案するか）＋ 店舗概要・Web状況・営業情報・営業メモ |
| `/leads/[id]` のサイト制作プロンプト | **その店のデータを差し込んだ制作プロンプトを生成**。コピーして Claude Code に貼ると `index.html` が出ます |
| `/playbook` 営業手順 | **7ステップの営業手順・電話トークの型・業種別の切り口・断られたときの対応・提案プラン一覧** |
| `/costs` コスト | 本日／今月の使用額、種別内訳、検索ログ、API使用履歴、予算の残り |
| `/metrics` 営業指標 | 発見 → 架電 → 到達 → 興味 → 提案 → 成約のファネルと成約率 |
| `/settings` 設定 | Lead Score の重みと優先度閾値を変更（コードに埋め込んでいません） |
| `/about` 利用目的 | 何のためにデータを集めているかの明示。削除依頼の窓口 |

---

## 先出し提案：作ってから見せる

実績のない事業者にとって最も効くのは、**「作りませんか」ではなく「もう作りました」**です。
前者は未来の約束なので相手は「頼むかどうか」を判断しますが、後者は既に存在するものなので
「捨てるかどうか」の判断になります。

写真は**作った後にもらいます**。先に許可と写真集めを求めると、まだ何も見ていない相手に
面倒な作業を頼むことになり、そこで止まるためです。

```
1. 写真なしで作る    公開情報だけで1ページを完成させる（相手に何も求めない）
2. 電話して見せる    「もう作りました。要らなければ消します」
3. 写真をもらう      完成物を見た後なので、写真提供が「参加」になる
4. 条件を出す        写真を入れて仕上げ、公開するか決めてもらう
```

手順の詳細と各段階の文面は `/playbook` にあります。
写真の使用許可を文面で残す手順、特定電子メール法の必須表記も含まれています。

### サイト制作プロンプト

リード詳細画面に、**その店のデータを差し込んだ制作プロンプト**が生成されます。
業種ごとに配色（HEX）・書体・セクション構成・禁止表現が変わります。

出力例は [`docs/examples/`](docs/examples/) にあります
（整体院向けのプロンプトと、それを通して出てきたサイト）。

プロンプトには以下が含まれます。

- **実データ**（店名・評価・口コミ件数・住所・営業時間）。不明な項目は「推測して書かないこと」と明示
- **業種別のデザイン指定**。「おしゃれに」では既定値が出るので、HEXまで指定します
- **捏造の禁止**。創業年・受賞歴・顧客の声・メニュー価格を勝手に作らせません
- **業種ごとの規制表現**（整体の「治る」、医療広告ガイドライン、景表法など）
- **AIが既定で出す型の禁止**（紫グラデーションのヒーロー、3カラムの特徴カード、架空の推薦文）
- **写真がない段階での作り方**。灰色のプレースホルダを禁止し、写真なしでも完成品に見せます
- **noindex と提案用サンプルである旨の表記**（合意前の公開事故を防ぐ）

---

## 売るもの：公式サイト（HP）と販売ページ（LP）

この2つは目的が違うので、同じ「HP制作」として提案すると刺さりません。

| | 公式サイト | 販売ページ（LP） |
|---|---|---|
| 目的 | 店名で検索されたときの**受け皿** | 1つの商品・コースを**売り切る** |
| 性質 | 常設。信頼性と基本情報 | 行動は1つだけ。広告やSNSから直接送れる |
| プラン | 1ページ型 / 5ページ型 / 予約対応 / リニューアル | 体験・相談申込 / 商品・コース販売 / キャンペーン |

**SNSで集客できている整体院に「5ページのHP」を提案しても「今で足りてる」で終わりますが、
「初回体験の申込ページ」なら来月の予約数の話になります。**

どちらを提案すべきかは、業種とWeb状況からルールベースで自動判定され、
各リードの詳細画面の「次の一手」に出ます（AI未設定でも表示されます）。

- 古いサイトがある → リニューアルが最優先（ゼロから作るより通りやすい）
- サイトが整備済み → サイトの提案は通らないのでLPに切り替え
- SNSのみ → 1ページ型から入る（最小構成の方が通しやすい）

### 価格

| | 月額あり（主力） | 買い切り |
|---|---|---|
| 1ページ型サイト | ¥98,000 + 月¥5,500 | ¥168,000 |
| 5ページ型サイト | ¥198,000 + 月¥8,800 | ¥298,000 |
| 体験・相談申込LP | ¥128,000 + 月¥5,500 | ¥198,000 |

**月額ありを主力に**しています。入口の金額が低く、店主はサーバー管理ができないので
結局必要になり、こちらには継続収入が残るためです。
買い切りは「サブスクが嫌い」な層（年配の店主に多い）向けの受け皿です。

日本の相場はフリーランスで制作費10〜50万円・月額保守5,000〜1万円、
中小の制作会社で80〜150万円。LP1ページ単体は中央値40万円で、
10万円以下は個人・フリーランスにしか頼めない価格帯とされています。

**実績がないうちほど安くしすぎない方が持ちます。** 1ページでも先出し制作・電話・修正・公開で
6〜8時間かかるので、¥98,000 なら時給14,000円で成立しますが、¥30,000 だと時給4,300円です。
それに先出し提案の時点で相手のリスクは既にゼロ（見てから決められる）なので、
そこへ低価格を重ねるのはカードの二重切りになります。

安くする場合は**理由と件数を明示した割引**にしてください
（「事例を作りたいので最初の5件まで ¥69,000」）。理由が終われば価格を戻せます。
無条件の安値は、最初の客の紹介客にも引き継がれて固定されます。

金額と根拠は `src/config/pricing.ts` にまとまっています。

### 小規模事業者持続化補助金

ウェブサイト制作費が補助対象（補助率2/3）で、¥98,000 なら約¥65,000が戻る計算になります。
**ただし先出し提案とそのままでは両立しません。**

> 補助対象になるのは「交付決定日以降に発注」した経費だけで、
> 交付決定前の発注・契約・支払いはさかのぼって対象にできません。
> つまり、作って渡してその場で契約・入金してもらうと、その費用は補助対象外になります。

そのため2トラックに分けています。

| | 通常トラック | 補助金トラック |
|---|---|---|
| 対象 | 今すぐ欲しい店舗 | 補助金を使いたい店舗 |
| 進め方 | 見せる → 契約 → 公開 | 見せる（**提案資料扱い**）→ 申請 → 交付決定 → **ここで受注** |
| 入金まで | 2〜4週間 | 半年〜1年 |

提案資料として完成品を見せること自体は問題がなく、むしろ申請書の具体性が上がるので
採択にも有利に働きます。

リード詳細画面に、業種から**対象／要確認／対象外**を自動判定するパネルが出ます
（医療法人・宗教法人・学校法人は制度上の対象外なので、話を持ち出さないよう表示を出し分けます）。

制度の内容・スケジュール・案内時の注意は `/playbook` の「補助金の活用」にまとめてあります。
データは `src/config/subsidy.ts`。**公募回ごとに条件が変わるので、次回は必ず更新してください。**

### 補助金の申請サポート（別事業）

HP営業とは別に、補助金の申請サポートを有料で行うための仕組みが入っています。
**主役は商工会議所・商工会（公式窓口）で、こちらはその手が回らない範囲を埋める位置づけです。**

**最初に読むべき制約：**
2026年1月1日施行の改正行政書士法により、**報酬を得て補助金の申請書類を作成することは
行政書士の独占業務**になりました。無資格で行うと第21条の罰則（1年以下の拘禁刑または
100万円以下の罰金）の対象になり得ます。判断は名目ではなく**実態**で行われるため、
契約書の書き方や顧問契約・会員サービスの形にしても免れません。
依頼した店舗側も、採択取消・返還請求のリスクを負います。

そこでこの事業は **「書かない」ことを設計の中心** に置いています。

| やってよい | やってはいけない |
|---|---|
| 制度の説明、公募要領の読み解き、審査の観点の解説 | 申請書の下書きを作って渡す |
| 対象になりそうかの診断、補助額の試算 | 店主の口述を聞いてこちらが打ち込む |
| 事業計画の壁打ち（**口頭で論点を返すまで**） | 事業計画書の添削・赤入れ |
| スケジュール管理・チェックリスト・窓口予約の段取り | 経費明細や交付申請書の作成 |
| 自社が提供するHP制作の見積書の作成 | |

「代わりに書いてほしい」と言われたら、行政書士に引き継ぎます。

**提供するサービス**（定額制。成功報酬は採りません — 書類作成の対価と見なされやすく、
また採択されないと0円になるため独占業務の線を越える圧力が自分にかかります）

| サービス | 料金 | 公式窓口では埋まらない部分 |
|---|---:|---|
| 補助金の対象診断 | 無料 | 「そもそも自分が対象か」が分からず窓口の前で止まっている |
| 事業計画の壁打ち | ¥49,800 | 中身をゼロから一緒に考える時間までは取りにくい |
| 段取りの管理 | ¥19,800 | 様式4の締切（申請締切の11日前）を個別に追ってはくれない |
| 採択後の段取り支援 | ¥29,800 | 実績報告の不備で減額・不交付になる。ここまで見る窓口は少ない |

**見合わないサポートは売らない仕組みが入っています。**
`isSupportWorthwhile()` は「補助額がサポート料の2倍以上あること」を条件にしており、
リード詳細のパネルが提案プランごとに判定を出し分けます
（1ページ ¥98,000 → 補助¥65,333 で**勧めない**／標準サイト ¥198,000 → 補助¥132,000 で提案可）。

見込み選定は `scoreSubsidyProspect()` が Lead Score とは別軸で行います。
法人格の確認が要る業種は、点数が高くても「要確認」で止めます。

営業文（`src/config/subsidyOutreach.ts`）は次を守っています。テストで固定してあります。

- 最初の接触では、**自分の有料サービスより先に商工会議所を案内する**
- **HP制作もしている利益相反を自分から開示する**
- 採択を保証しない（採択率47.2%・後払いで入金まで約1年を明記する）
- 書類作成をしないことを、契約前に明示する
- 偽の緊急性を使わない（**様式4の締切が申請締切より11日早い**という本物の締切だけを使う）

最終的にHP制作へは「ウェブ費用だけでは申請できません。他に何を申請しますか？」
という質問の答えとして繋がります。売り込みではなく、申請を成立させるために必要なものとして出てきます。

詳しくは `/playbook` の「補助金サポート事業」、設計の背景は `docs/DECISIONS.md` の第11章。

### 設定する場所

| 内容 | ファイル |
|---|---|
| プランの内容・納品物・期間 | `src/config/offerings.ts` |
| **価格・割引・補助金の扱い** | `src/config/pricing.ts` |
| 業種ごとの切り口・注意点 | `src/config/approaches.ts` |
| 営業手順・トークの型・かける時間帯 | `src/config/procedure.ts` |
| 先出し提案の4段階と各段階の文面 | `src/config/outreachFlow.ts` |
| **補助金の制度データ・対象判定** | `src/config/subsidy.ts` |
| **補助金サポート事業の法的境界・料金** | `src/config/subsidySupport.ts` |
| **補助金サポートの営業文・心理設計** | `src/config/subsidyOutreach.ts` |
| 業種別のサイトデザイン指針 | `src/config/siteDesign.ts` |

実際にかけてみて反応が違ったら、これらを書き換えてください。正解は地域と時期で変わります。

---

## データソースを切り替える

`.env.example` を `.env.local` にコピーして編集します。

### ビジネスデータ（店舗の発見）

| `BUSINESS_DATA_PROVIDER` | コスト | 評価・レビュー | 備考 |
|---|---|---|---|
| `mock`（デフォルト） | $0 | あり（架空） | キー不要。全機能の動作確認用 |
| `osm` | $0 | **なし** | OpenStreetMap。評価がないため中核フィルタが効かない |
| `dataforseo` | $0.012/req + $0.00036/件 | あり | 本番用。柏市1,300件で約 **$0.49** |

### Web検索（Phase 2 Stage 2・公式サイト候補の探索）

| `SEARCH_PROVIDER` | コスト | 備考 |
|---|---|---|
| `none`（デフォルト） | $0 | Stage 2をスキップ。「HPなし」判定の精度は落ちる |
| **`dataforseo`（有料なら推奨）** | **$0.002/クエリ** | **Business Listings と同じアカウント・請求・予算ガード** |
| `brave` | 約$4〜5/1,000クエリ | 別アカウント。DataForSEO SERP より高い |
| `google` | — | **新規申込不可**（2027年1月終了予定） |

無料で使える汎用Web検索APIは2026年9月時点で実質存在しません。有料を許容するなら
`SEARCH_PROVIDER=dataforseo` が最も安く、業者も1つで済みます。
詳細は [`docs/DECISIONS.md`](docs/DECISIONS.md) §7 を参照してください。

### メールアドレスについて

公式サイト上に事業用として公開されているメールのみを保存し、**一覧にアドレスをそのまま表示**します
（コピーボタン付き）。「メールあり」で絞り込めます。

- **推測生成はしません。** 店名やドメインから `info@` を組み立てるようなことは一切しません
- 見つからなければ `null` のままで、画面とCSVには「未確認」と表示します
  （空欄だと「調べていない」のか「調べたが無かった」のか区別できないため）
- アドレスにマウスを乗せると、**どのページで確認したか**が出ます。CSVにも出所URLの列があります
- フリーメール（gmail等）は、公式サイト上に事業用として掲載されている場合のみ保存します

メールが埋まるのは Phase 2（サイト解析）を実行した店舗だけです。
`SEARCH_PROVIDER` が `none` のままだと公式サイト候補の探索が行われないので、
データ提供元がサイトURLを返した店舗に限られます。

### 柏市を全件調査した場合の総額（有料構成）

| 段階 | 金額 |
|---|---|
| Phase 1（1,300件） | $0.49 |
| Phase 2 Stage 2（通過180件） | $0.36 |
| AI（Gemini無料枠なら$0） | $0〜0.09 |
| **合計** | **約 $0.85〜0.94** |

`PER_SEARCH_BUDGET_USD` のデフォルトは `1` なので、全件調査をするなら上げてください。

### AI（営業分析）

| `AI_PROVIDER` | コスト | 備考 |
|---|---|---|
| `heuristic`（デフォルト） | $0 | ルールベース。営業トーク・メール文面・HP提案まで生成する |
| `gemini` / `openai` / `anthropic` | 従量 | 店舗に即した文面になる |

---

## 守っていること

このツールは他人の店舗情報と他人のサーバーを扱います。以下は設定で緩められない形で実装しています。

- **予算の強制停止** — 月間・1検索あたりの上限を超える外部API呼び出しは、警告ではなく
  例外を投げて停止します。課金される `fetch` はすべて `BudgetGuard.spend()` の内側にあり、
  呼ぶ前ではなく**呼び出しごと**にガードがかかっています。
- **連絡先を推測生成しない** — 店名やドメインから `info@` を組み立てるようなことはしません。
  見つからなければ `null` で保存し、UIとCSVには「未確認」と表示します。
- **AI推定値と実データを分離** — AIの出力は専用のカラムに隔離し、画面には「AI生成（要確認）」と
  明示します。JSONのパースに3回失敗したら、埋めずに `null` のままにします。
- **robots.txt の遵守** — アクセス前に必ず取得・確認し、許可されていないパスには行きません。
  robots.txt が取得できなかった場合もアクセスを見送ります。
- **クロール間隔2秒以上** — 同一ドメインへの間隔は最低2秒（`Crawl-delay` がより長ければそちらに従う）。
  設定で2秒未満にはできません。User-Agent には識別子と連絡先URLを含めます。
- **Google Maps のスクレイピングをしない** — ブラウザ自動操作による取得は実装していません。
- **営業メールの自動送信をしない** — メールアドレスの取得・表示までです。
- **個人の私的メールを蓄積しない** — フリーメールは、公式サイト上に事業用として
  掲載されている場合を除いて保存しません。
- **補助金の申請書類を作成しない** — 改正行政書士法の独占業務に触れないよう、
  営業文には「書類の作成はしない・必要なら行政書士に引き継ぐ」ことを明示しています。
  やってよいこと／いけないことは `BOUNDARY_RULES` に列挙し、テストで固定しています。
- **採択を保証しない・無料の公式窓口を先に案内する** — 補助金の文面は、自分の有料サービスより
  先に商工会議所・商工会を案内し、HP制作をしている利益相反を自分から開示します。
  採択率と後払いも隠さず書きます。これもテストで固定しています。

---

## コマンド

```bash
npm run dev          # 開発サーバー
npm run build        # 本番ビルド
npm start            # 本番サーバー
npm test             # ユニットテスト（338件）
npm run lint         # ESLint
npm run check        # typecheck + lint + test をまとめて実行
npm run typecheck    # 型チェック
npm run db:init      # DBの初期化（初回起動時に自動で走るので通常は不要）
npm run db:reset     # DBを削除して作り直す
npm run check:dataforseo   # DataForSEO の疎通確認（要 login/password）
```

データは `data/localweb-hunter.db`（SQLite）に入ります。Node 22.5+ 組み込みの `node:sqlite` を
使っているので、ネイティブビルドも追加パッケージも不要です。

アプリを更新してスキーマが変わった場合は、起動時に自動でカラムが追加されます
（`src/lib/db/migrate.ts`）。集めた店舗データと営業メモは保持されるので、
DBを作り直す必要はありません。

---

## テストしていること

スコアリング・判定ロジック・重複排除のバグは「静かにリストの質を下げる」ため、重点的にテストしています。

| ファイル | 何を守っているか |
|---|---|
| `budgetGuard.test.ts` | 予算超過時に**呼び出す前に**止まること |
| `dedupe.test.ts` | テナントビル・チェーン本部番号で誤統合しないこと |
| `phone.test.ts` | 電話番号の正規化。整形できないものを無理に整形しないこと |
| `detection.test.ts` | ドメイン分類。`notgoogle.com` を Google と誤判定しないこと |
| `scoring.test.ts` | 「評価4.7・200件・HPなし」が「評価4.0・5件・HPなし」を明確に上回ること |
| `contactExtractor.test.ts` | メールが見つからないとき `null` を返すこと（捏造しない） |
| `robots.test.ts` | robots.txt の解釈。Allow/Disallow の優先順位、ワイルドカード |
| `csv.test.ts` | UTF-8 BOM 付きで出力すること（Excelの文字化け防止） |
| `phase1Filter.test.ts` | Phase 1 がAIを使わずルールだけで判定していること |
| `costEstimator.test.ts` | 2段階分離でPhase 2のコストが実際に減っていること |
| `jsonRetry.test.ts` | AI出力が3回失敗したら `null`（それらしい値で埋めない） |
| `subQueryPlanner.test.ts` | 全件調査モードの地点分割に穴が空かないこと |
| `repository.test.ts` | Phase 2 の対象を取りこぼさないこと、件数が過小報告されないこと |
| `phase2Batch.test.ts` | 実行時間の上限を超える前に自分で打ち切ること |
| `migrate.test.ts` | 既存DBを更新しても店舗データと営業メモを失わないこと |
| `nextAction.test.ts` | 業種とWeb状況に応じて、提案するものが正しく切り替わること |
| `contactDisplay.test.ts` | 表示するメールが必ず出所を持つこと |
| `robots.test.ts` | クロール間隔の例外が自分自身宛にしか効かないこと |
| `subsidy.test.ts` | 補助額の計算・対象判定と、採択を保証する文面が混ざらないこと |
| `subsidySupport.test.ts` | 申請書類の作成が禁止側に分類されていること、商工会議所を先に案内すること、利益相反を開示すること、見合わないサポートを勧めないこと |
| `emphasis.test.ts` | 文面の強調が記号のまま画面に出ないこと |

---

## ドキュメント

- [`docs/DECISIONS.md`](docs/DECISIONS.md) — **実装上の判断と、ブリーフから変わった前提**。まずここを読んでください
- [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) — アーキテクチャ、データフロー、DBスキーマ
- [`docs/CLAUDE_CODE_BRIEF.md`](docs/CLAUDE_CODE_BRIEF.md) — 元の実装指示

---

## 本番データに切り替えるときの手順

1. [DataForSEO](https://app.dataforseo.com/) でアカウントを作り、API の login/password を取得
2. `cp .env.example .env.local` して `DATAFORSEO_LOGIN` / `DATAFORSEO_PASSWORD` を設定
3. `npm run check:dataforseo` で疎通確認し、実レスポンスの構造を確認
4. 現在の単価を https://dataforseo.com/pricing/business-data/business-listings-api で確認し、
   `DATAFORSEO_COST_PER_REQUEST` / `DATAFORSEO_COST_PER_ITEM` を更新
5. `BUSINESS_DATA_PROVIDER=dataforseo` に変更
6. `CRAWLER_USER_AGENT` の連絡先URLを、自分の連絡先が分かるページに変更
7. `src/app/about/page.tsx` の削除依頼の連絡先を自分のものに書き換え
8. `MONTHLY_BUDGET_USD` / `PER_SEARCH_BUDGET_USD` を許容額に設定
9. まずは半径3km・単一業種など**小さい範囲**で1回実行し、実コストを `/costs` で確認してから広げる
